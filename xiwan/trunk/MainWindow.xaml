<Window x:Class="Gqqnbig.TrafficVolumeCalculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" d:DesignHeight="200"
        Title="MainWindow" Loaded="Window_Loaded" Height="700">
    <Window.Resources>
        <DataTemplate x:Key="histRTemplate">
            <ItemsControl Name="itemsControl"
                              ItemsSource="{Binding Path=HistR, Converter={StaticResource histogramConverter}, ConverterParameter=30}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Rectangle Width="1" Stroke="Red" VerticalAlignment="Bottom" Height="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DataTemplate>
        <DataTemplate x:Key="histGTemplate">
            <ItemsControl Name="itemsControl"
                              ItemsSource="{Binding Path=HistG, Converter={StaticResource histogramConverter}, ConverterParameter=30}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Rectangle Width="1" Stroke="Green" VerticalAlignment="Bottom" Height="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DataTemplate>
        <DataTemplate x:Key="histBTemplate">
            <ItemsControl Name="itemsControl"
                              ItemsSource="{Binding Path=HistB, Converter={StaticResource histogramConverter}, ConverterParameter=30}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Rectangle Width="1" Stroke="Blue" VerticalAlignment="Bottom" Height="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DataTemplate>
    </Window.Resources>


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Image Name="image1" Grid.Column="0" Stretch="None" />
        <ListView Name="contourListBox1" Grid.Column="1" ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  SelectionChanged="contourListBox_SelectionChanged">
            <ListView.View>
                <GridView >
                    <GridViewColumn>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox Checked="CheckBox_Checked" Unchecked="CheckBox_Unchecked" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="图片">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding Path=CarImage}" Width="30"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="R" CellTemplate="{StaticResource histRTemplate}" />
                    <GridViewColumn Header="G" CellTemplate="{StaticResource histGTemplate}" />
                    <GridViewColumn Header="B" CellTemplate="{StaticResource histBTemplate}" />
                </GridView>
            </ListView.View>
        </ListView>
        <TextBlock Grid.Column="2">
                <TextBlock.Inlines>
                    <Run>汽车总数量：</Run>
                    <Run Name="totalCarNumberTextRun"/>
                </TextBlock.Inlines>
        </TextBlock>

        <Image Name="image2" Grid.Row="1" Grid.Column="0" Stretch="None" />
        <ListView Name="contourListBox2" Grid.Row="1" Grid.Column="1" ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  SelectionChanged="contourListBox2_SelectionChanged">
            <ListView.View>
                <GridView >
                    <GridViewColumn>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox Checked="CheckBox2_Checked" Unchecked="CheckBox2_Unchecked" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="图片">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding Path=CarImage}" Width="30"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="R" CellTemplate="{StaticResource histRTemplate}" />
                    <GridViewColumn Header="G" CellTemplate="{StaticResource histGTemplate}" />
                    <GridViewColumn Header="B" CellTemplate="{StaticResource histBTemplate}" />
                </GridView>
            </ListView.View>
        </ListView>
        <TextBlock Grid.Row="1" Grid.Column="2">
                <TextBlock.Inlines>
                    <Run>汽车总数量：</Run>
                    <Run Name="totalCarNumberTextRun2"/>
                </TextBlock.Inlines>
        </TextBlock>
        
        <StackPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="3">
            <Button Content="计算直方图相似度" Click="Button_Click" />
            <TextBox Name="histSimilarityTextBox" AcceptsReturn="True" IsReadOnly="True"/>
        </StackPanel>
    </Grid>
</Window>
